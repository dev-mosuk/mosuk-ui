import { Header as HeaderBase } from './component';
import type { HeaderProps } from './component.interface';

export const Header = HeaderBase

export namespace Header {
  export type Props = HeaderProps;
}
