import { Observer as BaseObserver } from './component';
import type { ObserverProps } from './component.props';

export const Observer = BaseObserver;

export namespace Observer {
  export type Props = ObserverProps;
}
