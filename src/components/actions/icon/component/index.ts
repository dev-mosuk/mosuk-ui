import {
  Icon as BaseIcon
} from './component';
import type {
  IconProps
} from './component.interface';


export const Icon = BaseIcon;

export namespace Icon {
  export type Props = IconProps;
}
