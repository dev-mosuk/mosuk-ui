import {
  IconSkeleton as BaseIconSkeleton
} from './component';
import type {
  IconSkeletonProps
} from './component.interface';

export const IconSkeleton = BaseIconSkeleton;

export namespace IconSkeleton {
  export type Props = IconSkeletonProps;
}
