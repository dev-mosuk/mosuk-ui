import { Switch as Component } from './component';
import type { SwitchProps } from './component.interface';

export const Switch = Object.assign(Component, {});

export namespace Switch {
  export type Props = SwitchProps;
}
