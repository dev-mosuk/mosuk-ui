import { Skeleton as BaseSkeleton } from './component';
import type { SkeletonProps } from './component.interface';

export const Skeleton = BaseSkeleton;

export namespace Skeleton {
  export type Props = SkeletonProps;
}
